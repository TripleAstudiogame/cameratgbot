# Скопируйте этот файл в .env или запустите setup_server.bat — .env создастся сам.
# Не публикуйте .env в интернет.

SECRET_KEY=
PORT=6565
ALLOWED_HOSTS=*
ADMIN_USERNAME=Amir

# Оставьте пустым — тогда при первом запуске задаётся пароль по умолчанию (см. credentials.txt).
# ADMIN_PASSWORD_HASH=

MAIL_IMAP_HOST=imap.mail.ru

# LOG_LEVEL=INFO
